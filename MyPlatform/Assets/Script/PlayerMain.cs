using UnityEngine;
using System.Collections;

public class PlayerMain : PlayerHandler
{

    // Use this for initialization
    void Start()
    {
        PlayerStart();
    }
	
    // Update is called once per frame
    void Update()
    {
        HandlerUpdate();
    }
}
